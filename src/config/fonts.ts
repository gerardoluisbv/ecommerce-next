import { Lato, Montserrat_Alternates } from 'next/font/google';

export const inter = Lato({ 
  subsets: ['latin'], 
  weight: ['400', '700'] // Add appropriate weights here
});

export const titleFont = Montserrat_Alternates({ 
  subsets: ['latin'],
  weight: ['500', '700'],
});